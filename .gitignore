# Dependencies
node_modules/
.pnpm-store/

# Astro build output
dist/
.astro/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Lockfiles other than pnpm (we use pnpm)
package-lock.json
yarn.lock

# Post-sale delivery artifacts (kickoff / sprint / status / CO / milestone-gate /
# handoff / internal retros / financial burn). Contains live client data,
# milestone fees, change order pricing, and internal-only retros. NEVER commit.
delivery/

# Private pricing anchors used by the /proposal skill — contain real client
# contract values, names, and internal hourly rates. NEVER commit to a public repo.
.claude/skills/proposal/references/

# Internal marketing strategy — positioning, GTM playbooks, channel plans,
# competitive analysis, ICP targeting, budgets, and funnel/KPI data. Competitive
# and commercially sensitive. Lives in the repo for working use but is NEVER
# committed to this PUBLIC repo.
marketing/

# Productized "agency memory" template — the replicable project-based file/logic
# structure (design.md → records → AI-generated content) packaged as a sellable
# product for real-estate agencies. Internal product IP. Local working use only,
# NEVER committed to this PUBLIC repo.
agency/

# 依赖与构建产物：可重装/重建，不进 git
.next/
.cache/
__pycache__/
*.pyc
